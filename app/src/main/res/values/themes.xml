<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.SpotVault" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/sv_void</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="colorPrimary">@color/sv_primary</item>
        <item name="colorAccent">@color/sv_teal</item>
    </style>

    <!-- Used by the widget's quick-action relay activities — a translucent scrim over
         whatever was already on screen (usually the launcher) rather than a jarring opaque
         window while the camera/share flow does its thing. -->
    <style name="Theme.SpotVault.Transparent" parent="Theme.SpotVault">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>
